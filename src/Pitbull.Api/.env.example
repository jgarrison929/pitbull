# =============================================================================
# Pitbull API - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# For .NET, environment variables override appsettings.json using the
# double-underscore (__) convention: Section__Key = "value"
#
# Railway: set these in the Railway dashboard under service variables.
# Local dev: appsettings.Development.json handles most defaults.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------

# Full connection string for the Pitbull database
# Format: Host=<host>;Port=<port>;Database=<db>;Username=<user>;Password=<pass>
# Railway provides DATABASE_URL but we use the .NET config key below.
ConnectionStrings__PitbullDb=Host=localhost;Port=5432;Database=pitbull;Username=pitbull;Password=pitbull_dev

# -----------------------------------------------------------------------------
# JWT Authentication
# -----------------------------------------------------------------------------

# Issuer claim embedded in tokens (must match between API and consumers)
Jwt__Issuer=pitbull-api

# Audience claim embedded in tokens
Jwt__Audience=pitbull-client

# Secret signing key (MINIMUM 32 characters, change in production!)
Jwt__Key=CHANGE-ME-IN-PRODUCTION-minimum-32-characters-long

# Access token lifetime in minutes (default: 60)
Jwt__ExpirationMinutes=60

# Refresh token lifetime in days (default: 7)
Jwt__RefreshExpirationDays=7

# -----------------------------------------------------------------------------
# CORS
# -----------------------------------------------------------------------------

# Allowed origins for the Production CORS policy (comma-separated in config,
# but set as indexed env vars for .NET array binding)
# In development, the "Dev" CORS policy allows all origins.
Cors__AllowedOrigins__0=https://pitbull-web-production.up.railway.app
Cors__AllowedOrigins__1=http://localhost:3000

# -----------------------------------------------------------------------------
# ASP.NET Core Runtime
# -----------------------------------------------------------------------------

# Environment: Development | Staging | Production
# Controls middleware behavior (Swagger, CORS policy, error detail)
ASPNETCORE_ENVIRONMENT=Development

# URL bindings (Railway sets PORT automatically)
# In Docker/Railway: http://+:${PORT:-8080}
# Local dev default: http://localhost:5000
ASPNETCORE_URLS=http://localhost:5000

# -----------------------------------------------------------------------------
# Serilog Logging
# -----------------------------------------------------------------------------

# Override minimum log level (Default, Debug, Information, Warning, Error, Fatal)
Serilog__MinimumLevel__Default=Information

# Per-namespace overrides
Serilog__MinimumLevel__Override__Microsoft.AspNetCore=Warning
Serilog__MinimumLevel__Override__Microsoft.EntityFrameworkCore=Warning

# -----------------------------------------------------------------------------
# Docker / Railway
# -----------------------------------------------------------------------------

# Port the app listens on (Railway injects this automatically)
# PORT=8080
