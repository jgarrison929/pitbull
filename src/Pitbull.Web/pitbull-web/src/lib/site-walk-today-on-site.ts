/** Site walk today-on-site: same project API (3.3.4) */
export const SITE_WALK_TODAY_ON_SITE_HINT =
  "Today's field activity uses the same project API - no second aggregation.";
export function siteWalkTodayOnSiteEnabled(): boolean {
  return true;
}
